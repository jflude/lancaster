# LANCASTER - things that still need to be done

- not yet portable (especially sync.h) - requires gcc or clang

- support DNS lookups on addresses passed as arguments to programs (modify the
  sock.c module to provide an API)

- develop "easy" READER & WRITER example programs using the batch functions

- integrate the Go interface & utilities into the autoconf build system

- remove proprietary features from the Java & Python interfaces, and integrate

