#! /bin/sh

PAUSE="-pause 1"
STATS=""
VM_MAX_HB=20

GOPATH=~/go
export GOPATH

cd
mkdir -p /plogs/submgr
exec submgr/latest/scripts/startSubMgr `hostname | sed -n 's/^pv.*$/-missed '$VM_MAX_HB'/p'` $PAUSE $STATS
