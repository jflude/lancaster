#! /bin/sh

pause="-pause 1"
stats=""
vm_max_hb=20

GOPATH=~/go
export GOPATH

cd
mkdir -p /plogs/submgr
exec submgr/latest/scripts/startSubMgr $pause $stats `hostname | sed -n 's/^pv.*$/-missed '$vm_max_hb'/p'`
